import React, { Component } from 'react';
import { View, Text } from 'react-native';

class ScannerComponent extends Component {
  constructor(props) {
    super(props);
    this.state = {
    };
  }

  render() {
    return (
      <View>
        <Text> ScannerComponent </Text>
      </View>
    );
  }
}

export default ScannerComponent;
